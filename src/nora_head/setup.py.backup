# Copyright 2026 nanostation
# Licensed under the Apache License, Version 2.0

from setuptools import setup
import os
from glob import glob

package_name = 'nora_head'

setup(
    name=package_name,
    version='0.1.0',
    packages=[package_name],
    data_files=[
        ('share/ament_index/resource_index/packages',
            ['resource/' + package_name]),
        ('share/' + package_name, ['package.xml']),
        # Launch files (will add in Story 17)
        # (os.path.join('share', package_name, 'launch'), glob('launch/*.py')),
        # Config files (will add in Story 17)
        # (os.path.join('share', package_name, 'config'), glob('config/*.yaml')),
    ],
    install_requires=['setuptools'],
    zip_safe=True,
    maintainer='nanostation',
    maintainer_email='nanostation@todo.todo',
    description='Nora robot head - multimodal AI perception and interaction',
    license='Apache-2.0',
    tests_require=['pytest'],
    entry_points={
        'console_scripts': [
            # Will add node entry points in future stories:
            # 'camera_driver = nora_head.nodes.camera_driver:main',
            # 'pico_bridge = nora_head.nodes.pico_bridge:main',
            # etc.
        ],
    },
)
